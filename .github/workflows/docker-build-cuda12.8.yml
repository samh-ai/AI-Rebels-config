name: Build and Push Docker Image (CUDA 12.8)

on:
  push:
    branches: [ "main" ]
    paths:
      - 'docker/Dockerfile.cuda12.8'
      - 'docker/boot.sh'
      - '.github/workflows/docker-build-cuda12.8.yml'
  workflow_dispatch:

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile.cuda12.8
          push: true
          tags: airebels/comfyui:cuda12.8
